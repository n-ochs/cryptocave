import React from 'react'

const NewsList = () => {
    return (
        <div>
            news list - rendering news into a standard card format, maybe have one large one with the rest smaller beneath. sorting based on upvotes, recent, specific coins etc. include functionality to 'pin' articles aka same logic as coin watchlist but call it saved articles or something.
        </div>
    )
}

export default NewsList
